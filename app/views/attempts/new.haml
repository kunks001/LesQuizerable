.col-md-12.banner_image
  .col-md-4.col-md-offset-1#banner-text-container
    #banner-text 
      Have you got what it takes to Learn to Code?
.col-md-12.dark-geometric
  .row.quiz-title
    .col-md-10.col-md-offset-1
      / = @quiz.title
      Take this simple quiz to see if you have the raw ingredients to become a Developer...!
  .col-md-10.col-md-offset-1
    .background-lining
    .col-md-12.quiz-background
      %form{id: "attempts_form", name: "/attempts/#{@quiz.id}"}
        / .show_quiz
        /   .quiz_title
        - @quiz.questions.each do |question|
          .row.question-row
            .row
              .col-md-12.question-text
                = question.question_text
            .col-md-6
              - if question.image
                - image = question.image.filename
                %img{src: "https://s3.amazonaws.com/MakersQuizApp/#{image}"}
            .col-md-6
              - question.answers.each do |answer|
                - if answer.image
                  .col-md-6.answer-input
                    .answer-image
                      - image = answer.image.filename
                      %img{src: "https://s3.amazonaws.com/MakersQuizApp/#{image}"}
                    %label.answer-response
                      = answer.response
                    .input
                      %input{type: "radio", name: "answer_ids[#{question.id}]", value: "#{answer.id}" }
                - else
                  .col-md-12.answer-input
                    %label.answer-response
                      = answer.response
                    .input
                      %input{type: "radio", name: "answer_ids[#{question.id}]", value: "#{answer.id}" }
        .col-md-12#show-score-container
          .submit-quiz-button
            %input{type: "submit", value: "Submit", class: "btn btn-success", id: "submit_quiz"}
          #show-score
